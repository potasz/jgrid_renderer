package jgrid.sunflow.renderer;

/**
 * Defines two constants to specify the type of the Sunflow renderer
 * to use in the remote worker tasks.
 * 
 * @author Szabolcs Pota
 * @version 0.1.2
 * @since 0.1
 */
public interface RendererTypes {

    public static String SIMPLE_RENDERER = "SIMPLE";

    public static String BUCKET_RENDERER = "BUCKET";
}
